# 🎨 Guide du Design System & Référentiel des Styles — AnkiForge

Ce document constitue la **source unique de vérité (Single Source of Truth)** pour l'architecture visuelle, le système de design, la gestion des modes **Sombre 🌙 / Clair ☀️**, la matrice de correspondance des tokens pour chaque composant UI et l'inventaire des **12 Familles de Thèmes bivalentes (24 thèmes au total)** d'AnkiForge.

> [!IMPORTANT]
> **Règle d'or de Contribution UI & Nouveaux Composants :**
> Toute création d'un nouveau type de composant, widget interactif ou panneau dans AnkiForge **DOIT IMPÉRATIVEMENT** être répertoriée dans la **Partie 1** de ce document avec la spécification exacte des variables de styles et `DesignTokens` à consommer. Aucun style ni code couleur hexadécimal ne doit être codé en dur dans le code source Qt PySide6.

---

# 📐 PARTIE 1 : Référentiel des Tokens & Standards par Composant

Le moteur de style centralisé (`StyleEngine`) et la classe `DesignTokens` (`src/ankiforge/ui/theme.py`) exposent des constantes sémantiques dynamiques. Lors d'un changement de thème ou de mode (Sombre 🌙 / Clair ☀️), ces tokens sont réassignés à chaud et le QSS global est recompilé.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           StyleEngine & ThemeFamily                         │
│             Mode : 🌙 Sombre / ☀️ Clair ⟷ 12 Familles Bivalentes            │
│                                      │                                      │
│                                      ▼                                      │
│           DesignTokens (bg_main, bg_panel, accent_primary, is_dark...)      │
│                                      │                                      │
│             ┌────────────────────────┴────────────────────────┐             │
│             ▼                                                 ▼             │
│    Global QSS (generate_stylesheet)            Widgets Spécifiques (Qt)     │
│    - QPushButton[role="..."]                   - TabButton / IdeTabBar      │
│    - QLineEdit, QTextEdit, QComboBox           - StyledMenu / Context Menu  │
│    - QTabWidget, QTabBar, QTableView           - Badges, Sliders, Docks     │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 1.0 Tokens de Teintes Sémantiques (Alpha) & Branches A/B

Pour toute surface « teintée » (badge, pastille, fond de statut, diff, hover sémantique), **aucun `rgba(...)` ni hexadécimal ne doit être codé en dur** : utiliser les tokens dérivés ci-dessous (définis dans `DesignTokens`, dérivés à chaud via `_with_alpha` et les champs optionnels du `ThemeProfile` dans `apply_theme_profile`).

| Token | Rôle | Valeur (Sombre 🌙) | Valeur (Clair ☀️) |
| :--- | :--- | :--- | :--- |
| `ACCENT_BG` / `ACCENT_BORDER` | teinte accent (fond / bordure) | `rgba(99, 102, 241, 0.15)` / `rgba(99, 102, 241, 0.3)` | dérivé de `color_accent` |
| `COLOR_RED_BG` / `COLOR_RED_BORDER` | erreur, danger, suppressions diff | `rgba(239, 68, 68, 0.15)` / `rgba(239, 68, 68, 0.3)` | dérivé de `color_red` |
| `COLOR_GREEN_BG` / `COLOR_GREEN_BORDER` | succès, validations, insertions diff | `rgba(16, 185, 129, 0.15)` / `rgba(16, 185, 129, 0.3)` | dérivé de `color_green` |
| `COLOR_YELLOW_BG` / `COLOR_YELLOW_BORDER` | avertissement, état intermédiaire | `rgba(245, 158, 11, 0.15)` / `rgba(245, 158, 11, 0.3)` | dérivé de `color_yellow` |
| `COLOR_BLUE_BG` / `COLOR_BLUE_BORDER` | information, focus explicite | `rgba(59, 130, 246, 0.15)` / `rgba(59, 130, 246, 0.3)` | dérivé de `color_blue` |
| `COLOR_PURPLE_BG` / `COLOR_PURPLE_BORDER` | lien « branche A », variantes `cloze` | `rgba(99, 102, 241, 0.12)` / `rgba(99, 102, 241, 0.3)` | dérivé de `color_purple` |
| `COLOR_RED_TEXT` | texte sur teinte rouge | `#f87171` | `color_red` de base |
| `COLOR_GREEN_TEXT` | texte sur teinte verte | `#6ee7b7` | `color_green` de base |
| `COLOR_YELLOW_TEXT` | texte sur teinte jaune | `#fcd34d` | `color_yellow` de base |
| `COLOR_BLUE_TEXT` | texte sur teinte bleue | `#93c5fd` | `color_blue` de base |
| `COLOR_PURPLE_TEXT` | texte sur teinte violette | `#a5b4fc` | `color_purple` de base |
| `BRANCH_A_BG` / `BRANCH_A_BORDER` | variante violette rôle/étape A | `rgba(139, 92, 246, 0.12)` / `rgba(139, 92, 246, 0.45)` | dérivé de `color_purple` |
| `BRANCH_B_BG` / `BRANCH_B_BORDER` | variante cyan rôle/étape B | `rgba(6, 182, 212, 0.12)` / `rgba(6, 182, 212, 0.45)` | dérivé de `color_cyan` |

> [!NOTE]
> Les couleurs de marque des fournisseurs IA (`#4285F4` Google, `#10a37f` OpenAI, `#d97706` Gemini) et les blancs de texte inversés (`#ffffff`) sur fond accent/coloré sont des exceptions volontaires (identité / contraste), non converties en tokens.

---

## 1.1 Matrice de Correspondance : Composants ➔ Tokens Sémantiques

| Famille de Composant | Classe / Sélecteur Qt | Propriété Visuelle | Variable de Style / Token Requis | Description & Règle Métier |
| :--- | :--- | :--- | :--- | :--- |
| **Boutons Primaires** | `PrimaryButton`, `QPushButton[role="primary"]` | Arrière-plan<br>Arrière-plan Hover<br>Texte<br>Bordure / Radius | `accent_primary`<br>`accent_hover`<br>`#ffffff`<br>`border: none`, `radius_sm` (6px) | Action principale d'une vue (ex: Forger, Sauvegarder). Toujours mis en valeur avec l'accent. |
| **Boutons Secondaires** | `SecondaryButton`, `QPushButton[role="secondary"]` | Arrière-plan<br>Hover<br>Bordure<br>Texte | `bg_input`<br>`bg_hover`<br>`border_color`<br>`text_primary` | Actions neutres / secondaires (ex: Annuler, Parcourir, Filtrer). |
| **Boutons Danger** | `DangerButton`, `QPushButton[role="danger"]` | Arrière-plan<br>Hover<br>Texte / Bordure | `rgba(239, 68, 68, 0.14)`<br>`rgba(239, 68, 68, 0.28)`<br>`color_red` | Actions destructrices (ex: Supprimer la note, Éliminer le doublon). |
| **Boutons Fantômes / Icônes** | `IconButton`, `QPushButton[role="ghost"]` | Arrière-plan<br>Hover<br>Icône / Texte | `transparent`<br>`bg_hover`<br>`text_secondary` ➔ `text_primary` | Boutons compacts de barres d'outils, fermetures et toggles. |
| **Champs de Texte & Épis** | `QLineEdit`, `QTextEdit`, `QPlainTextEdit`, `StyledTextEdit` | Arrière-plan<br>Bordure normale<br>Bordure focus<br>Texte / Sélection | `bg_input`<br>`border_color`<br>`border_focus`<br>`text_primary` / `accent_primary` | Saisie de code, prompts, filtres de recherche et texte de cartes. |
| **Listes Déroulantes** | `QComboBox`, `StyledComboBox` | Arrière-plan champ<br>Menu Popup<br>Item Hover / Select | `bg_input`, bordure `border_color`<br>`bg_panel`, bordure `border_color`<br>`bg_hover`, texte `accent_primary` | Sélecteurs de modèles IA, paquets, pipelines et formats. |
| **Onglets de Panneaux IDE** | `TabButton`, `IdeTabBar` | Inactif<br>Hover<br>Actif (`:checked`)<br>Accents sémantiques d'icônes (`icon_color`) | Fond `bg_sidebar`, texte `text_secondary`<br>Fond `bg_hover`, texte `text_primary`<br>Fond `bg_panel`, texte `accent_primary`, barre supérieure 2px `accent_primary`<br>Icônes teintées dynamiquement (`color_yellow`, `color_blue`, `color_green`, `color_purple`) | Titre et indicateur de l'onglet actif dans les panneaux multifenêtres. Supporte l'injection d'accents sémantiques sur les icônes (`icon_color`), la persistance lors des détachements/rattachements de fenêtres et la mise à jour réactive lors des switches de thèmes via `set_tab_icon_color`. |
| **Onglets Documents** | `TabButton[variant="document"]` | Inactif<br>Actif (`:checked`) | Fond `transparent`, bordures `border_color`<br>Fond `bg_input`, texte `accent_primary`, barre haute `accent_primary` | Onglets de documents ouverts dans l'éditeur de cours. |
| **Onglets Pill / Segments** | `PillTabBar`, `QTabBar::tab` | Inactif<br>Actif (`:selected`) | Fond `bg_input`, texte `text_secondary`<br>Fond `bg_main`, texte `accent_primary`, bordure basse 2px `accent_primary` | Sous-navigation interne (ex: onglets de persona, onglets de pipeline). |
| **Menus Déroulants & Contextuels** | `QMenu`, `StyledMenu` | Arrière-plan menu<br>Bordure<br>Item normal<br>Item sélectionné<br>Séparateur | `bg_panel`<br>`1px solid border_color`, `radius_md` (10px)<br>Texte `text_primary`, padding 7px 20px 7px 32px<br>Fond `bg_hover`, texte `accent_primary`<br>`1px solid border_color` | Menus clic-droit (onglets, tableaux de cartes, personas, pipelines). |
| **Panneaux & Docks IDE** | `IdePanel`, `QFrame[card-style="panel"]` | Fond conteneur<br>En-tête (header)<br>Bordure globale | `bg_panel`<br>`bg_sidebar`, bordure basse `border_color`<br>`1px solid border_color`, `radius_md` (10px) | Panneaux modulaires détachables et empilables style JetBrains. |
| **Tableaux & Grilles** | `QTableWidget`, `QTableView`, `StyledTableWidget` | Fond grille<br>En-têtes colonnes<br>Cellule sélectionnée<br>Lignes de grille | `bg_panel`, texte `text_primary`<br>`bg_sidebar`, texte `text_muted`, bordure `border_color`<br>`bg_active`, texte `text_primary`<br>`border_color` | Tableau des cartes générées, liste des règles de linter, historique. |
| **Drapeaux Anki (Flags 1 à 7)** | `FlagItemDelegate`, `FLAG_COLORS` | 1: Rouge (`#ef4444`)<br>2: Orange (`#f97316`)<br>3: Vert (`#10b981`)<br>4: Bleu (`#3b82f6`)<br>5: Rose (`#ec4899`)<br>6: Turquoise (`#06b6d4`)<br>7: Violet (`#a855f7`) | Mât vectoriel et fanion coloré peints en QPainter 60 FPS. Outline subtil au survol si sans drapeau (`flag == 0`). | Rendu graphique vectoriel instantané des drapeaux de cartes Anki sans instanciation de widget Qt, sélection interactive au clic et raccourcis `Ctrl+0` à `Ctrl+7`. |
| **Plan de Document (Outline)** | `DocumentOutlineWidget`, `OutlineItemDelegate` | Badges H1 (`accent_bg`/`accent_primary`), H2 (`color_blue_bg`/`color_blue_text`), H3 (`color_purple_bg`/`color_purple_text`), H4+ (`bg_hover`/`text_muted`)<br>Scroll Spy : fond `accent_bg`, barre gauche 3px `accent_primary`<br>Métriques : `text_muted`<br>Filtres profondeur : `bg_input`, checked `accent_primary` | Délégué QPainter haute performance 60 FPS pour l'arborescence Markdown avec navigation bidirectionnelle éditeur (Scroll Spy), filtres de profondeur H1-H3, pastilles de couverture SRS et action Forger en 1-clic. |
| **Arborescences & Listes** | `QTreeWidget`, `QListWidget`, `QTreeView` | Fond<br>Item Hover<br>Item Sélectionné | `bg_panel` (ou `transparent` dans sidebar)<br>`bg_hover`<br>`bg_active`, texte `accent_primary` (ou `text_primary`) | Explorateur de documents, hiérarchie de dossiers de personas. |
| **Cases à Cocher & Radios** | `QCheckBox`, `QRadioButton` | Décoché<br>Hover<br>Coché (`:checked`) | Fond `bg_input`, bordure `border_color`<br>Bordure `accent_primary`<br>Fond `accent_primary`, bordure `accent_primary` | Sélections multiples, options de prompt, filtres d'état. |
| **Interrupteurs & Lignes d'Options** | `ToggleSwitch`, `OptionToggleRow` | Track OFF / ON<br>Bordure / Thumb<br>Carte hover | `bg_input` / `accent_primary`<br>`border_color` / `#ffffff`<br>Fond `bg_panel`, hover `border-color: accent_primary` | Toggles modernes style iOS/macOS pour réglages, vision multimodale et validation automatique. |
| **Curseurs & Sliders** | `QSlider` | Gorge (groove)<br>Partie active (sub-page)<br>Curseur (handle)<br>Handle Hover | `bg_input`, bordure `border_color`<br>`accent_primary`<br>`#ffffff`, bordure 2px `accent_primary`<br>`accent_hover` | Réglages continus (Température LLM, Max Tokens, Ratios). |
| **Barres de Défilement** | `QScrollBar:vertical`, `QScrollBar:horizontal` | Fond rail<br>Curseur (handle)<br>Handle Hover | `transparent`, largeur 10px<br>`border_color`, `radius: 5px`<br>`text_muted` | Ascenseurs discrets et harmonieux pour le contenu long. |
| **Badges Sémantiques** | `Badge` | Variantes `neutral`, `success`, `warning`, `danger`, `info` | Fond `bg_active` ou teinté sémantique (`color_green`, `color_yellow`, `color_red`, `color_blue`), texte contrasté | Étiquettes d'état (ON/OFF, Validé, Conflit, Erreur, IA). |
| **Fenêtres & Modales** | `QDialog`, `QMainWindow` | Fond de fenêtre<br>En-tête modale | `bg_main`<br>`bg_panel`, bordure basse `border_color` | Paramètres généraux, History Time Machine, Modale de fusion. |
| **Disposition Fluide & Wrap** | `FlowLayout`, `FlowWidget` | Marges & Espacements | `h_spacing: 6px`, `v_spacing: 6px` | Disposition fluide avec passage à la ligne automatique et propagation `heightForWidth` pour les pilules, tags et badges dans les `QScrollArea`. |
| **Barres d'Actions Responsives** | `ResponsiveTopActionBar` | Arrière-plan<br>Bordure<br>Boutons compacts | `bg_sidebar`<br>`1px solid border_color`, `border-top: 1px solid border_light`<br>Bascule automatique en icônes seules sous 480px | Barre supérieure compacte (38px) avec titre et actions qui s'adaptent dynamiquement aux petits écrans (13" MacBook). |
| **Éditeurs de Code Professionnels** | `CodeEditorWithGutter`, `NativeCodeEditor`, `LineNumberArea`, `LintStatusBar` | Gouttière numéros<br>Ligne courante<br>Linter soulignements<br>Barre statut linter | `bg_sidebar`, bordure droite `border_color`, numéros `text_muted` / `accent_primary`<br>Fond `bg_hover`<br>`color_red` (erreurs) / `color_yellow` (warnings) en `WaveUnderline`<br>Fond `bg_sidebar`, bordure haute `border_color`, pastilles sémantiques | Éditeur HTML/CSS avec gouttière native QPainter, pastilles de couleur interactives, auto-fermeture de balises (<test> -> <test></test>), autocomplétion contextuelle (`{{...}}`, `<...>`, CSS) et formateur de code. |
| **Éditeurs de Champs de Notes & IntelliSense** | `NoteFieldEditorWidget`, `NoteFieldTextEdit`, `NoteKaTeXHighlighter`, `NoteKaTeXCompleter` | Gouttière compacte<br>Coloration KaTeX/HTML/Cloze<br>Popup autocomplétion<br>En-tête pliable | `bg_panel`, bordure droite `border_color`<br>HTML (`#38bdf8`), LaTeX (`#34d399`), Jinja2 (`#fbbf24`), Cloze (`#c084fc`)<br>Popup `bg_panel`, hover `bg_active`<br>`bg_panel`, texte `text_primary` / `accent_primary` | Champs d'édition 100% natifs Qt pour les modèles de cartes Anki avec IntelliSense (LaTeX sur `\`, HTML sur `<`, champs/Cloze sur `{{`), auto-fermeture de balises et raccourcis universels. |
| **Barre d'Outils d'Édition Extensible & Personnalisable** | `EditorToolbarWidget`, `ToolbarAction`, `ToolbarCustomizeDialog` | Fond barre<br>Boutons d'action<br>Menu trois points (overflow)<br>Dialogue de personnalisation<br>Séparateurs adaptatifs | `bg_panel`, bordure `border_color`<br>`IconButton` 24px avec infobulle enrichie et raccourci<br>`IconButton("dots-three-vertical")` avec `StyledMenu`<br>`ToolbarCustomizeDialog` avec cartes de catégories et cases à cocher<br>`1px solid border_color` avec masquage automatique des séparateurs orphelins | Barre d'outils unifiée au sommet de l'éditeur agissant sur le champ actif (Gras, Italique, Souligné, Barré, Code, Math, Cloze, Liens, Images, Listes) avec API d'extension publique `register_action()`, menu trois points (⋮) pour actions masquées et dialogue modale de personnalisation persistée dans `SettingsService`. |
| **Machine à Remonter le Temps** | `TimeMachineDialog`, `DiffViewerWidget` | Timeline latérale<br>Diff comparatif<br>Aperçu rendu | `bg_panel`, items avec bordure active `accent_primary`<br>Fond `bg_input`, ajouts `rgba(16, 185, 129, 0.15)` (+), suppressions `rgba(239, 68, 68, 0.15)` (-)<br>`CardPreviewWidget` intégré | Modale moderne d'historique de versions avec timeline chronologique, diff interactif champ par champ et bouton de restauration 1-clic mettant à jour la BDD et l'éditeur en direct. |
| **Disposition Progressive Disclosure** | `EditionView`, `main_splitter (Vertical)`, `nav_ribbon` | Tableau haut (Pleine largeur)<br>Ruban compact (30px)<br>Éditeur / Preview bas (50/50) | Fond `bg_main`, tableau avec texte épuré sans HTML brut et tags colorés<br>Ruban `bg_panel` avec boutons Précédent/Suivant et dépliage<br>Fond `bg_sidebar`, split 50/50 champs et `CardPreviewWidget` | Architecture supérieure/inférieure ergonomique combinant tableau pleine largeur et repliement 1-clic en ruban de navigation (30px) libérant 100% de la hauteur utile pour l'éditeur de champs et le rendu live. |
| **Smart Merge 3 Panneaux** | `SmartMergeDialog`, `SmartMergeView`, `ConflictFieldRow` | Panneau Local (Gauche)<br>Panneau Fusionné (Centre)<br>Panneau Entrant (Droite)<br>Badge similarité | Fond `rgba(239, 68, 68, 0.06)`, bordure rouge (30%)<br>Fond `bg_input`, bordure `accent_primary`<br>Fond `rgba(16, 185, 129, 0.06)`, bordure verte (30%)<br>Badge sémantique vert/jaune/rouge selon ratio C-Bridge | Dialogue de résolution de conflits de contenu selon la Règle 11 avec transfert par champ `[◀ Local]` / `[▶ Entrant]` et pagination de conflits. |
| **Dialogues d'Importation & Exportation** | `ImportDialog`, `ImportDropZone`, `ExportDialog` | Zone Glisser-Déposer<br>Options & Radios<br>Boutons d'action | Fond `bg_input`, bordure tiretée `border_color`, hover `accent_primary`<br>Cadre `bg_panel`, bordure `border_color`<br>`PrimaryButton` avec icônes Phosphor | Modales unifiées pour l'ingestion (.apkg, .colpkg, .txt) et l'exportation sélective avec médias Zstandard. |
| **Coloration Syntaxique Code** | `HTMLSyntaxHighlighter`, `CSSSyntaxHighlighter` | Balises & Sélecteurs<br>Attributs & Propriétés<br>Chaînes & Textes<br>Mots-clés & Conditions<br>Variables & Champs Anki<br>Commentaires<br>Nombres & Unités | `syntax_tag`<br>`syntax_attr`<br>`syntax_string`<br>`syntax_keyword`<br>`syntax_variable`<br>`syntax_comment`<br>`syntax_number` | Tokens thématiques pour la coloration syntaxique temps réel dans les éditeurs HTML / Jinja2 / CSS. |
| **Pastilles & Formateurs de Code** | `CSSFormatter`, `HTMLFormatter`, `extract_colors_from_text` | Pastilles de couleur gouttière<br>Bouton Formater (barre statut)<br>Raccourcis | Rendu dynamique de la couleur réelle avec bordure 1px<br>Bouton compact `bg_panel`, hover `accent_primary`<br>`Ctrl+Alt+L` (JetBrains), `Ctrl+Shift+I`, `Shift+Alt+F` | Aperçu visuel immédiat des couleurs hex/rgb/hsl et formateur automatique réordonnant indentation, sauts de ligne et espacements. |
| **Graphique d'Activité 7 Jours** | `ActivityChartWidget` | Barres créées (accent)<br>Barres modifiées (bleu)<br>Rails colonnes<br>Labels jours | `accent_primary`<br>`color_blue`<br>`border_light` (hover: `bg_hover`)<br>`text_muted` (hover: `accent_primary`), `font_size_small` | Rendu 100% natif QPainter sans dépendance lourde, affichant le volume quotidien de cartes créées vs modifiées avec infobulle interactive au survol. |
| **Diagnostics & Actions Proactives** | `ProactiveDiagnosticsWidget`, `DiagnosticCardWidget` | Cartes d'alertes<br>Bordures sémantiques<br>Boutons d'action | `bg_panel` (hover: `bg_hover`)<br>`color_yellow` (warning), `color_red` (danger), `border_color` (info)<br>`PrimaryButton` / `SecondaryButton` compacts | Cockpit de supervision proactive avec détection automatique des violations Wozniak, lacunes RAG, doublons et surcoûts avec redirection 1-clic. |
| **Menu de Notifications & Diagnostics** | `NotificationMenuPopup`, `NotificationItemWidget` | Menu Popup<br>Badges de sévérité<br>Bouton cloche | `bg_panel`, bordure `border_color`, ombre portée `shadow_glass_blur`<br>`bg_active`, pastille colorée<br>`IconButton` avec infobulle dynamique | Menu déroulant rattaché à la cloche TopBar listant les anomalies actives avec pastille de comptage et routage interactif vers les vues et sous-onglets. |
| **Gestionnaire d'Addons & Extensions** | `AddonManagerWidget`, `AddonDetailWidget`, `AddonConfigForm` | Liste addons<br>Formulaire dynamique<br>Badges de statut<br>Documentation | `bg_panel`, bordure `border_color`<br>Champs `bg_input`, bordures `border_color`<br>`Badge` sémantique (`success`, `neutral`, `danger`)<br>`bg_panel`, texte `text_primary` | Vue de gestion des extensions sous Nuitka avec formulaire interactif généré automatiquement depuis `config.json`, visualiseur de doc `config.md` et installateur ZIP. |
| **Historique des Personas (Time Machine IA)** | `PersonaHistoryDialog`, `PersonaPromptDiffViewer`, `PersonaVersionItemWidget` | Timeline versions<br>Diff de prompt<br>Paramètres & Métas | `bg_panel`, items avec bordure `border_light` et hover `accent_primary`<br>Fond `bg_input`, ajouts `rgba(16, 185, 129, 0.15)` (+), suppressions `rgba(239, 68, 68, 0.15)` (-)<br>Tableau de métas `bg_input` | Modale d'historique de versions pour les Personas/Agents IA avec comparaison de diffs colorés et restauration en 1 clic. |
| **Modèles Communautaires & Paquets .afmodel** | `ModelExportDialog`, `ModelImportDialog`, `StarterPackDialog`, `StarterModelCardWidget` | Formulaire export<br>Aperçu import WebEngine<br>Catalogue Starter Pack | `bg_panel`, bordure `border_color`<br>Splitter 50/50 avec `CardPreviewWidget`<br>Grille responsive de cartes avec badges `accent_primary` | Suite modale complète pour exporter, inspecter et importer des gabarits de cartes (.afmodel, .json) avec résolution de collisions de styles et catalogue de 4 packs intégrés. |
| **Boutons & Pastilles Jinja2 (Pipelines)** | `TagPillButton` | Fond teinté<br>Bordure pill<br>Texte monospace | `field` (indigo), `cloze` (violet), `warning` (ambre), `success` (émeraude), `info` (cyan)<br>`border-radius: 12px`, `padding: 1px 10px`<br>`font_code`, hover `1.5px solid accent_primary` | Pastilles d'insertion rapide de snippets de variables Jinja2 avec retours visuels et disposition fluide `FlowWidget`. |
| **Sous-Onglets IDE & Barres Segmentées** | `SegmentedTabBar`, `SegmentedTabButton`, `SubTabButton` | Inactif<br>Hover<br>Actif (`:checked` / `is_active=True`)<br>Badges d'état | `transparent`, texte `text_secondary`<br>Fond `bg_hover`, texte `text_primary`<br>Fond `bg_panel`, texte `text_primary`, bordure basse `2px solid accent_primary` (mode `subtab`) ou fond `accent_primary` (mode `segmented`)<br>Pastille pill `accent_primary` ou `bg_active` | Barre de sous-onglets unifiée avec support des variantes `subtab` (relief et indicateur d'accent bas style IDE) et `segmented` (gélule compacte), navigation intégrale au clavier (touches fléchées), badges sémantiques dynamiques et rétrocompatibilité ascendante avec l'ancienne API `SubTabButton`. |
| **Bannière de Flux DAG & Diagnostic** | `DagFlowOverviewWidget`, `StatusPillBadge` | Nœuds d'étapes<br>Chevrons de liaison<br>Badge de santé DAG | Fond `bg_input` (actif: `rgba(99, 102, 241, 0.15)`), bordure `border_color`<br>Icône Phosphor `ph.caret-right` (12x12)<br>Badge pill (`border-radius: 9999px`) vert (Valide) ou rouge (Alerte) | Visualisation interactive du graphe orienté acyclique avec navigation instantanée par clic sur nœud et linter de variables. |
| **Cartes d'Étapes de Workflow (Maître)** | `StepItemWidget`, `InlineInsertButton` | Ligne 1 : En-tête<br>Ligne 2 : I/O & Actions<br>Sélectionné (`is_selected=True`)<br>Bouton d'insertion `+` | Poignée `dots-six-vertical` + Icône Phosphor + Titre bold + Badge de rôle pill<br>Flux `📥 in ➔ 📤 out` monospace + boutons compacts (20px) `▲`, `▼`, `🗑`<br>Fond `bg_active`, `border-left: 3px solid accent_primary`<br>Bouton rond 20px centré `+` avec trait pointillé | Ligne de liste de workflow compacte (2 lignes ergonomiques) sans débordement horizontal avec manipulation drag/reorder/delete. |
| **Catalogue & Tuiles d'Étapes** | `StepPickerDialog`, `StepPickerCard` | Recherche rapide<br>Tuile d'étape / Agent<br>Badges de catégorie | `GlowLineEdit` avec loupe intégrée<br>Fond `bg_panel`, bordure `border_color`, hover `border-color: accent_primary`<br>`Badge` pill stylé selon le rôle (LLM, RAG, Parallèle, Pause, Outil) | Modale de sélection d'étape en 2 colonnes (Agents IA vs Actions Système) avec filtrage instantané temps réel. |
| **Bannière KPIs de Branche A/B** | `BranchKpiWidget`, `apply_pill_style` | Titre Branche<br>Métriques (temps, cartes, tokens, coût)<br>Badge Vainqueur (Winner)<br>Statut | Capsule pill teinté (`BRANCH_A` = violet `#8b5cf6` pour A, `BRANCH_B` = cyan `#06b6d4` pour B — tokens `DesignTokens.BRANCH_A/B`)<br>Labels monospace `text_secondary` avec icônes Phosphor (`ph.timer`, `ph.cards`, `ph.coins`, `ph.currency-dollar`), aucune émoticône<br>Badge pill vert (`color_green`) `x.x x plus rapide` / `+N cartes` / `Meilleur coût / carte` / `Score global supérieur`<br>Badge pill `bg_input` / `border_color` | Bandeau structuré en 2 lignes (En-tête de branche + Ruban de métriques) affichant en temps réel (curseur live via QTimer) les performances comparatives avec détection multi-critères du gagnant (vitesse, volume de cartes, coût par carte, tokens). |
| **Laboratoire A/B — Cockpit Splitter & Résumé de Config** | `ABTestsView`, `SourceBox`, `ConfigPanel`, `ConfigSummaryBar`, `FlowLayout`, `DocumentEditorWidget` | Progressive disclosure « 2 écrans » (Configuration → Résultats)<br>Splitter horizontal `QSplitter` Source 55% \| Paramètres 45% (texte source `Expanding` pleine hauteur)<br>Import document modal (`DocumentPickerButton`)<br>Flip Recto/Verso par panneau<br>Réglages d'inférence toujours visibles (global + `chk_independent` A/B)<br>Barre de résumé de configuration persistée sur l'écran Résultats | `bg_panel`, bordure `border_color`, blocs `ConfigBlock` (`bg_input`), sélecteurs `StyledComboBox`<br>`DocumentEditorWidget` réutilisé de la vue Création (éditeur monospace `font_code`, compteur tokens, bouton « Coller », bouton « Générer » masqué) au lieu d'un `QPlainTextEdit` local — document dynamique via `set_document()` : vue PDF / Rendu Stylisé / Source Markdown selon `file_type` (toggle masqué en texte libre)<br>`DocumentPickerButton` → `DocumentSelectWindow`<br>Curseurs `QSlider` (Température, Tokens) persistés via `SettingsService` (`ab_test/*`), réglage commun (`chk_independent` décoché) OU réglages A/B (`chk_independent` coché) — jamais les deux à la fois, couleur slot `accent_primary` / `BRANCH_A` / `BRANCH_B`<br>Barre `ConfigSummaryBar` (2 lignes, hauteur auto `min-height 40`, plus de `fixedHeight`), bordure basse `accent_primary`, badges pill monospace (`font_code`, `border-radius: 9999px`, texte élidé vers 220px) dans un `FlowLayout` | Laboratoire comparatif A/B haute performance (Modèle vs Modèle, Prompt vs Prompt, Pipeline vs Pipeline) à écran unique : Écran 1 « Configuration » (splitter Source \| Paramètres : mode + paquet + modèle + agent/moteur commun + **bloc « Branches à comparer »** (Moteur A/B, Prompt A/B, Pipeline A/B — les paramètres réellement testés sont réglables avant le run) + réglages inférence + CTA Lancer), Écran 2 « Résultats » auto après le run (KPIs, 4 niveaux de vue dont Diff A↔B `DiffViewerWidget`, pagination sincro `chk_sync_nav`, flips par panneau, **nom de chaque branche en lecture seule** `lbl_branch_a/b`). La barre `config_summary_bar`, sur 2 lignes (bouton retour + titre, puis badges wrap dans un `FlowLayout`), rappelle la configuration active (Mode, Deck, Modèle, branches A/B, températures/tokens) avec retour « Modifier la configuration ». Aucun import de cartes dans la Forge depuis ce laboratoire (décision « Aucun import A/B ») ; la configuration gagnante est réutilisée dans Create/Batch. Exemples sources (`SampleSelectWindow`/`PRESET_SAMPLES`) supprimés. |
| **Centre de Configuration & Paramètres** | `SettingsModal`, `SettingsCard`, `SettingsNavButton`, `PasswordLineEdit`, `StorageMetricCard`, `AnkiSyncTab`, `StorageMaintenanceTab` | Fenêtre 960x640px<br>Cartes avec scoping QSS strict<br>Boutons sidebar avec bordure accent<br>Champs mot de passe avec œil<br>Cartes métriques de stockage | `bg_main`, `bg_panel`, bordure `border_color`<br>`QFrame#SettingsCard` évitant les cascades parasites<br>`border-left: 3px solid accent_primary`<br>`PasswordLineEdit` avec bouton toggle `ph.eye`<br>Statistiques réelles (BDD SQLite, Médias, Time Machine) avec actions VACUUM, nettoyage orphelins et snapshots | Fenêtre de paramètres modulaire en 5 onglets thématiques (Général, Moteurs IA, Anki & Synchro, Maintenance, Extensions) réactive à chaud aux 24 thèmes sombres/clairs. |
| **Sélecteur d'Espaces de Travail & Profils** | `ProfileSelectorDialog`, `ProfileItemWidget`, `QFrame#ProfileItemCard` | Cartes profils<br>Badge profil actif<br>Bordure sélection<br>Actions créations / suppression | `bg_panel`, `border_color`, hover `bg_hover`<br>`bg_active`, `color_green`<br>`1.5px solid accent_primary`<br>`StyledLineEdit`, `DangerButton`, `PrimaryButton` | Dialogue moderne de bascule d'espaces de travail et profils SQLite isolés avec cartes stylisées, filtrage instantané et sécurisation de la suppression. |
| **Virtualisation & Rendu Vectoriel 60 FPS** | `VirtualTableView`, `VirtualListView`, `BasePaginatedPeeweeModel`, `NoteVirtualTableModel`, `VirtualChunkListModel`, `BadgeItemDelegate`, `TagItemDelegate`, `CheckboxItemDelegate`, `ProgressBarItemDelegate`, `SimilarityBadgeDelegate`, `SrsMasteryDelegate`, `TextSnippetDelegate` | Tableau & liste virtualisés<br>Délégués vectoriels `QPainter`<br>Pagination Peewee<br>Sélection massive O(1) | `bg_panel`, `bg_sidebar`, `border_color`, `radius_md`<br>`DesignTokens.ACCENT_PRIMARY`, `COLOR_GREEN`, `COLOR_RED`, `COLOR_YELLOW`, `FONT_CODE`<br>Pagination dynamique par paquets (`canFetchMore`/`fetchMore`) sans allocation `QWidget`<br>Checkbox et badges dessinés vectoriellement sans lag | Architecture Model/View virtualisée capable de charger et défiler instantanément sur des collections de 100 000+ notes et fragments RAG à 60 FPS constants, avec prefetch groupé Peewee et sélection O(1). |
| **Sidebar des Discussions Consultant IA** | `ConsultantSessionSidebar`, `SessionItemWidget` | Volet latéral<br>Bouton Nouveau<br>Filtre rapide<br>Item de session<br>Footer stats | Fond `bg_sidebar`, bordure droite `border_color`<br>`PrimaryButton` accentué 28px<br>`StyledLineEdit` compact<br>Hover `bg_hover`, actif `bg_input` + bordure `accent_primary`<br>Fond `bg_input`, texte `text_muted` / `color_green` | Volet de navigation historique 3-panneaux style Cursor/Windsurf avec filtrage instantané, dates relatives, menu 3-points (renommer, exporter Markdown, supprimer) et compteurs de tokens/cartes. |
| **Accordéon Pliable de Réflexion ReAct** | `ChatMessageWidget`, `accordion_toggle_btn`, `steps_wrapper` | Bouton accordéon<br>Conteneur pensées<br>Icône chevron | Fond `bg_input`, bordure `border_color`, texte `text_muted` (hover: `accent_primary`)<br>Marge 0, espacement 6px<br>`ph.caret-right` (replié) / `ph.caret-down` (déplié) | Accordéon ergonomique compact masquant ou révélant la chaîne de raisonnement ReAct et les appels d'outils après exécution sans encombrer le flux de lecture conversationnel. |
| **Hub de Contexte & Scope de l'Agent IA** | `ContextHubWidget`, `ContextAssetCard`, `RawContextDialog` | Volet de travail droit<br>Cartes d'actifs connectés (verrouillage dynamique `is_committed`)<br>Jauge tokens & breakdown (/context)<br>Autonomie ReAct | Fond `bg_panel`, cartes `bg_input`, bordure `border_color`<br>Badge de tokens monospace `font_code`, icônes Phosphor, cadenas `ph.lock-simple`<br>Jauge `QProgressBar` avec chunks accentués, commande `/context`<br>Autonomie proactive ReAct | Cockpit de mémoire de travail pour l'Agent Aidant IA : gestion du scope persistant (Deck, RAG, Cartes), verrouillage des sources engagées dans l'historique, estimation granulaire des tokens et autonomie complète des appels MCP. |
| **Garde-Fou & Refactorisation Inline IA** | `InlineDiffCardWidget`, `FieldDiffWidget`, `SplitCardItemWidget` | Sélecteur de mode mémorisé (Boutons `📑 Vue Unifiée` vs `⫴ Vue Côte à côte`)<br>Unification Totale propre sans ratures par défaut + toggle `[ 🔍 Ratures ]`<br>Mode Côte à côte 2 colonnes (Actuel vs Proposition IA éditable)<br>Édition directe inline (`QTextEdit` synchronisé)<br>Scission atomique avec sélection (`SplitCardItemWidget`)<br>Bandeau d'intention IA<br>Navigation 1-clic Éditeur (`request_navigation`)<br>Détection automatique d'état en base de données | Fond `bg_input`, bordure `accent_primary`<br>Boutons de mode segmented `bg_panel` / hover `bg_hover` / actif `accent_primary`<br>Colonnes côte à côte `bg_input` avec bordures rouge/vert<br>Unification totale : vert doux `rgba(34, 197, 94, 0.18)` sans texte barré ; Ratures : suppressions rouge barré `rgba(239, 68, 68, 0.2)`<br>Bandeau `bg_panel` avec bordure gauche `accent_primary`<br>Boutons `PrimaryButton` (Appliquer/Revert), `SecondaryButton` (Rejeter), `QPushButton` (Ouvrir dans l'Éditeur ↗) | Composant conversationnel de Garde-Fou humain pour la validation de refactorisations : persistance inter-sessions du mode de vue (`QSettings` / `SettingRepository`), synchronisation instantanée inter-cartes, bascule instantanée entre Vue Unifiée (Unification Totale épurée avec toggle `[ 🔍 Ratures ]`) et Vue Côte à côte, détection automatique en BDD pour éliminer tout résidu de diff obsolète, comparaison granulaire par champ (Recto, Verso), retouche directe au clavier, sélection de cartes filles lors de scissions, annulation (revert) 1-clic et bouton direct `Ouvrir dans l'Éditeur ↗`. |
| **Visualiseur d'Albums & Planche Contact** | `AlbumViewerWidget`, `AlbumPageCard`, `PageInspectorWidget`, `AlbumImportDialog` | Grille contact sheet responsive<br>Vignette de page<br>Inspecteur zoomable & contraste<br>Dialogue import & tri | Fond `bg_main`, défilement fluide `FlowLayout`<br>Fond `bg_panel`, bordure `border_color`, hover `accent_primary`, badge `P. N`<br>Splitter avec zoom, curseur contraste et `QTextEdit` OCR<br>Boîte modale `bg_main` avec sélecteur de tri alphanumérique naturel / EXIF | Module complet de gestion et visualisation des supports de cours numérisés page par page (fiches manuscrites, polycopiés, manuels photographiés), avec rotation 90°, réordonnancement, suppression, transcription IA asynchrone et compilation PDF. |
| **Lecteur Audio Synchronisé** | `AudioPlayerWidget` | Barre de transport compacte<br>Curseur temporel glissable<br>Contrôle vitesse (0.75x-2.0x)<br>Boutons saut +-10s<br>Volume & sourdine | Fond `bg_panel`, bordure `border_color`, radius `radius_md`<br>Slider horizontal `color_green`<br>`QComboBox` vitesse compacte `bg_input`<br>`IconButton` Phosphor (`ph.play`, `ph.pause`, `ph.arrow-counter-clockwise`)<br>Label temporel `font_code` | Mini-lecteur audio multimédia natif Qt (`QMediaPlayer` + `QAudioOutput`) synchronisé avec les transcriptions de cours d'amphi, dictaphones et podcasts scientifiques, avec saut instantané au clic sur les fragments du sommaire. |
| **RAG Visuel & Indexation Multimodale** | `VisualRAGService`, `RAGTestDialog`, `AlbumViewerWidget` | Indexation sémantique sans OCR<br>Recherche multimodale FAISS+BM25<br>Vignettes visuelles dans les résultats<br>Boutons RAG dans l'Album | Fond `bg_panel`, badge visuel `🖼️ [Visuel]`, vignettes 36x36px `radius_sm`<br>Boutons `SecondaryButton` `ph.eye` (#10b981) et `IconButton` `ph.magnifying-glass`<br>Badge de pertinence %, RRF et statut d'indexation | Moteur de recherche et d'indexation visuelle dense (Option A - VLM Dense Indexing) permettant d'interroger directement des planches anatomiques, schémas, diaporamas et figures complexes sans transcription OCR préalable, avec association dynamique des médias originaux et prévisualisation directe. |
| **Sélecteur & Explorateur de Modèles LLM** | `ModelSelectorWidget`, `ModelCapabilityBadgesWidget`, `ModelDiscoveryDialog`, `ModelCardWidget`, `CapabilityPill`, `FilterChipButton` | Sélecteur compact enrichi<br>Pilules de capacités<br>Modale d'exploration & catalogue<br>Cartes de modèles (`ModelCardWidget`, `ModelCardWidget[current="true"]`)<br>Barre de filtres fournisseur & chips (`FilterChipButton`, `QFrame#FilterCard`)<br>Tiroir de comparaison matriciel (`QFrame#ComparePane`, `QFrame#CompareModelColumn`) | Fond `bg_input`, bordure `border_color`, hover `border_focus`<br>Badges colorés : Vision (cyan `#06b6d4`), Thinking (violet `#a855f7`), Vitesse (jaune `#eab308`), Contexte (indigo `#6366f1`), Gratuit/Local (vert `#10b981`), Coût (ambre `#f59e0b`)<br>Dialogue `bg_main`, filtres rapides, recherche en direct `GlowLineEdit`<br>Cartes `bg_panel` avec hover `accent_primary`, badge `[✓ Actuel]` vert et bordure `color_green` pour le modèle actif<br>Boutons d'action `PrimaryButton` (32px), sélection rapide par double-clic<br>Tiroir comparateur côte à côte 2-3 modèles avec colonnes matricielles distinctes et mise en valeur des atouts clés (vitesse, coût, contexte) | Composant universel de sélection et découverte de modèles LLM intégrant l'indication claire du modèle actuellement configuré, le filtrage instantané par fournisseur (Gemini, Claude, OpenAI, Ollama, Groq) et par capacités (chips interactives), l'inspection 1-clic des capacités (Vision, CoT, Contexte 2M, Tarification, 100% Local), la sélection rapide par double-clic ou bouton calibré, et un comparateur matriciel de coûts et spécifications. |
| **Boîte de Retours & Suggestions** | `FeedbackDialog`, `BugReportTab`, `FeatureIdeaTab` | Modale 780x640px<br>Onglets segmentés<br>Champs de saisie & sélecteurs<br>Boutons d'action (Copier, Exporter, GitHub) | `bg_main`, `bg_panel`, bordure `border_color`<br>Onglets `bg_input` (sélectionné : `bg_panel` + barre basse 2px `accent_primary`)<br>Champs `bg_input`, bordure `border_color`, focus `accent_primary`<br>`PrimaryButton` avec ombre accentuée, `SecondaryButton` compacts | Modale bivalente de retour utilisateur intégrant le signalement de bugs avec diagnostics système et logs anonymisés sanitisés, la suggestion d'idées de fonctionnalités et la redirection 1-clic vers GitHub Issues. |
| **Graphique Donut de Répartition** | `DonutChartWidget` | Anneau QtCharts (`QPieSeries` hole 0.55)<br>Label central HTML (total + %)<br>Légende basse<br>Survol avec explosion des parts | Fond `transparent`, texte centre `FONT_MAIN`<br>Bordure des parts `palette(base)`<br>Palette fixe 8 couleurs de données (non sémantique, hors tokens) | Graphique en anneau natif réutilisable pour les répartitions (ex: tokens par étape, répartition par source) avec interaction au survol : explosion des slices et mise à jour du label central. |
| **Plan de Document & Sommaire Reactif** | `DocumentOutlineWidget` | Champ de recherche `GlowLineEdit`<br>`QTreeWidget` 2 colonnes (Titre / Ligne)<br>Barre d'actions structurelles<br>État vide « Aucun titre détecté » | `BG_INPUT`, `TEXT_MUTED`, `BORDER_COLOR`, `BG_PANEL`, `RADIUS_SM`, `TEXT_PRIMARY`, `BG_HOVER`, `ACCENT_PRIMARY` (`QTreeWidget::item`, `:hover`, `:selected`)<br>Action Réparer (jaune `COLOR_YELLOW`), générer TOC (bleu `COLOR_BLUE`) | Arborescence Markdown interactive des titres avec filtrage temps réel, saut à la ligne au clic et actions d'optimisation structurelle (réparation des titres, génération de sommaire TOC). |
| **Dialogue de Réparation des Titres Markdown** | `RepairHeadingsDialog` | En-tête explicative avec icône clé `ph.wrench` jaune<br>`QTableWidget` 5 colonnes (Appliquer, Ligne, Titre, Niveau, Raison)<br>Cases à cocher individuelles par anomalie<br>Boutons « Tout cocher », « Tout décocher », « Annuler », « Appliquer » | `BG_PANEL`, `BORDER_COLOR`, `RADIUS_MD`, `BG_INPUT`, `TEXT_PRIMARY`, `TEXT_SECONDARY`, `TEXT_MUTED`, `RADIUS_SM`, `COLOR_YELLOW`<br>Boutons `PrimaryButton` (`ph.check`), `SecondaryButton` (`ph.x`) | Dialogue modal interactif de prévisualisation et confirmation des réparations de structure Markdown : affiche les sauts anormaux détectés, permet la sélection granulaire et garantit la non-destructivité avec annulation Undo `Ctrl+Z`. |
| **Tour Guidé d'Onboarding** | `TourBubble`, `TourStep` | Bulle flottante translucide avec ombre<br>Carte `QWidget#TourBubbleBg` bordure accent<br>Compteur `x/y` en `FONT_CODE`<br>Boutons Quitter/Précédent/Suivant | `BG_PANEL`, `ACCENT_PRIMARY`, `RADIUS_LG`, `TEXT_PRIMARY`, `TEXT_MUTED`, `FONT_CODE`<br>Flèches Phosphor blanches (contraste sur fond accent) | Guidage utilisateur multi-étapes positionné intelligemment autour du widget cible (droite→gauche→dessous avec clamping écran), fin de tour persistée dans `QSettings`. |
| **Toasts & Notifications Flottantes** | `Toast`, `ToastManager`, `ToastLevel` | `QFrame#ToastCard` bordure gauche 4px sémantique<br>Barre de progression temporelle (3px)<br>Fade in/out + empilement bas-droit (max 3)<br>Pause au survol | `COLOR_GREEN`/`COLOR_BLUE`/`COLOR_YELLOW`/`COLOR_RED` (niveaux success/info/warning/error)<br>`RADIUS_MD`, `BG_PANEL`, `BORDER_COLOR`, `TEXT_PRIMARY`, `TEXT_MUTED`, `BG_HOVER`, `TEXT_SECONDARY`<br>Piste progress `rgba(255, 255, 255, 0.15)` | Notifications non-bloquantes avec 4 niveaux sémantiques, auto-dismiss temporisé et survol pour mise en pause ; utilisées pour les retours d'action (sauvegardes, exports, erreurs). |
| **Éditeur de Notes KaTeX Natif** | `KaTeXEditor`, `KaTeXHighlighter`, `KaTeXCompleter`, `KaTeXTextEdit` | Toggle 3 modes (Texte/Mixte/Aperçu) dans pilule QSS<br>Splitter éditeur (code) / aperçu web sécurisé<br>IntelliSense macros<br>Debounce 500ms | `BG_PANEL`, `BORDER_COLOR`, `TEXT_MUTED`, `ACCENT_PRIMARY`, `FONT_CODE`, `FONT_SIZE_CODE`, `FONT_MAIN`, `FONT_SIZE_BASE`, `TEXT_PRIMARY`, `BG_MAIN`, `BG_HOVER`<br>Highlighter : `COLOR_BLUE` (HTML), `COLOR_GREEN` (LaTeX), `COLOR_YELLOW` (Jinja2), `COLOR_PURPLE` (Cloze)<br>CSS d'aperçu injecté (body, h1-h3, code, pre, blockquote, table, `.cloze`) | Éditeur de champs de cartes pleinement natif Qt avec rendu LaTeX/KaTeX en direct, coloration HTML/LaTeX/Jinja2/Cloze, autocomplétion des macros et aperçu Markdown désinfecté Anti-XSS dans une vue web sécurisée. |
| **Carte de Fragment Documentaire** | `SegmentItemWidget`, `SegmentInspectorWidget` | Case à cocher custom 14px (icône `check_white.svg`)<br>Badge `#N` en `ACCENT_BG`<br>Badge « ~N tok. » vert/jaune/rouge (seuils 2500/5000)<br>Extrait 2 lignes + texte barré si désactivé | `TEXT_MUTED`, `BG_INPUT`, `ACCENT_PRIMARY`, `TEXT_PRIMARY`<br>`COLOR_GREEN`, `COLOR_YELLOW`, `COLOR_RED`, `FONT_CODE`<br>`QCheckBox::indicator` (+ `:hover`, `:checked`) | Carte d'un fragment documentaire incluant/excluant le segment de la génération de cartes (clic sur la carte = bascule, case à cocher dédiée), volume estimé en tokens et représentation barrée quand exclu. |
| **Courbe de Rétention FSRS-4.5** | `RetentionCurveCanvas` | Rendu pur QPainter (`paintEvent`)<br>Grille 100/90/80%<br>Ligne cible 90% en pointillés verts<br>Aire sous courbe en dégradé indigo<br>Points clés J0/J1/J7/J30/J90 | `BG_MAIN`, `BORDER_COLOR`, `FONT_MAIN`, `TEXT_MUTED`<br>`COLOR_GREEN` (ligne cible), `ACCENT_PRIMARY` (courbe 2.5px), `COLOR_YELLOW` (points)<br>Dégradé aire : `QColor(99, 102, 241, 60)` → transparent | Visualiseur de la courbe de l'oubli (modèle FSRS-4.5) : rétention projetée sur 90 jours, cible 90% et aire sous la courbe, utilisé dans l'audit Wozniak pour juger la qualité des révisions. |
| **Rideau de Transition de Thème** | `ThemeTransitionOverlay`, `SpinningIconLabel` | Overlay semi-transparent (`rgba(10, 12, 16, 0.75)`)<br>Carte centrale 380×150 (icône palette rotative + titre)<br>`QProgressBar` indéterminé 4px accent<br>Fondu in/out + auto-nettoyage | `BG_PANEL`, `BORDER_COLOR`, `RADIUS_MD`, `ACCENT_PRIMARY`, `TEXT_PRIMARY`, `TEXT_MUTED`, `FONT_MAIN`, `BG_INPUT`<br>`QFrame` (carte), `QProgressBar::chunk` | Effet « rideau » pendant l'application d'un thème : fondu d'entrée, exécution du callback d'application entre les deux fondu, puis rotation stoppée et event filter retiré automatiquement. |
| **Cellule de Progression Batch** | `ProgressTableCellWidget` | Barre `QProgressBar` 5px colorée par état<br>Ligne statut + « N% » en `FONT_CODE` gras | `BG_INPUT`, `FONT_CODE`, `TEXT_MUTED`<br>Chunk coloré injecté (`ACCENT_PRIMARY` par défaut, `COLOR_GREEN` en succès)<br>`QProgressBar::chunk` | Cellule de tableau compacte affichant la progression d'une tâche du lot (statut textuel + pourcentage alignés sous la barre). |
| **Onglet Fusion de Doublons IA** | `AIDuplicatesMergeTab`, `DuplicateMatrixTable`, `DuplicateMergeInspector` | Matrice des paires suspectes<br>Bouton sélection paquet + relancer<br>Label « N paires à examiner »<br>Inspecteur de fusion (masqué si rien de sélectionné) | Composition pure — styling délégué aux sous-widgets `duplicate_widgets.py` (`ACCENT_PRIMARY`, `BG_ACTIVE`, `BG_INPUT`, `BG_MAIN`, `BG_PANEL`, `BORDER_COLOR`, `COLOR_BLUE/GREEN/RED/YELLOW`, `FONT_CODE`, `FONT_MAIN`, `TEXT_MUTED/PRIMARY/SECONDARY`)<br>Déchargement WebEngine à `hideEvent` | Onglet de scan et résolution des doublons d'un paquet : détection via `DuplicateWorker`, matrice des paires, fusion/ignoration versionnée (`NoteVersionModel`, `IgnoredDuplicateModel`). |
| **Inspecteur de Document & Couverture** | `DocumentInspectorPanel`, `ClickableChunkWidget` | Splitter 2 volets : sommaire + aperçu section, cartes liées<br>Badge « Couverture : X% » teint vert/jaune/rouge<br>Cartes « Trou de cours » en `dashed` jaune | `BG_MAIN`, `BG_PANEL`, `BORDER_COLOR`, `RADIUS_MD`, `TEXT_PRIMARY`, `COLOR_BLUE`, `COLOR_GREEN`, `COLOR_YELLOW`, `BG_INPUT`, `RADIUS_SM`, `BG_HOVER`, `TEXT_MUTED`, `TEXT_SECONDARY`, `ACCENT_PRIMARY`<br>`COLOR_GREEN_BG/BORDER`, `COLOR_YELLOW_BG/BORDER` (badges et paragraphes de diagnostic)<br>`ClickableChunkWidget:hover` → `ACCENT_PRIMARY` | Panneau d'inspection détaillée : sommaire des sections avec couverture (● vert / ○ jaune), aperçu de l'extrait, cartes Anki liées et actions (forger les cartes manquantes, ré-indexer FAISS, synchroniser les fiches via tags). |
| **Aperçu de Carte Multi-Appareils** | `FlashcardPreview` | Barre supérieure de navigation (‹ › + compteur `N / M`)<br>`Badge` statut de validation<br>Rendu `CardPreviewWidget` (WebEngine + MathJax, modes Bureau/Tablette/Mobile) | `BG_PANEL` (fond composant), `FONT_CODE`, `TEXT_MUTED`<br>Statut délégué au `Badge` sémantique | Composant d'inspection et validation des cartes générées : navigation carte par carte, compteur, statut de validation et rendu multi-appareils. |
| **En-tête de Dossier Personas** | `FolderHeaderWidget` | Icône Phosphor 15px typée par rôle (tray / sous-dossier / dossier)<br>Nom en poids variable (bold/semi-bold)<br>`Badge` neutre de compte | Icône `TEXT_MUTED` (racine), `ACCENT_PRIMARY` (sous-dossier), `COLOR_YELLOW` (dossier)<br>Nom `TEXT_PRIMARY`<br>`background: transparent` | Ligne d'un dossier ou sous-dossier dans l'arbre des Personas : icône contextuelle, nom et compteur d'éléments. |
| **Écran Vide de l'Atelier Personas** | `PersonaEmptyStateWidget` | Carte centrale max-width 580px<br>Icône `ph.sparkle` accent 52px<br>2 CTA (parcourir modèles / créer agent vierge)<br>Mini-guide d'onboarding 3 étapes en `dashed` | `BG_MAIN`, `BG_PANEL`, `BORDER_COLOR`, `RADIUS_MD`, `ACCENT_PRIMARY`, `TEXT_PRIMARY`, `TEXT_MUTED`, `BG_INPUT`, `RADIUS_SM`<br>`QFrame` (container + guide_frame) | Écran d'accueil de l'Atelier de Personas lorsqu'aucun agent n'est sélectionné : explication du concept, deux actions rapides et guide d'onboarding en 3 étapes. |
| **Badge de Statut Sémantique avec Icône** | `StatusBadge`, `Badge`, `TagButton` | Pill `border-radius: 9999px`<br>Icône Phosphor 12px + texte 10px bold letter-spacing 0.4px<br>6 variantes (success/warning/info/danger/error/neutral)<br>`refresh_theme(profile)` dynamique | `ACCENT_PRIMARY`, `BG_PANEL`, `BORDER_COLOR`<br>Variantes : `COLOR_GREEN*`, `COLOR_YELLOW*`, `COLOR_BLUE*`, `COLOR_RED*` (+ `_BG`/`_BORDER` assortis), `TEXT_MUTED` | Pill badge élégant icône+texte avec variantes sémantiques teintées (fond + bordure assortis), indicateur de statut des onglets récolorisé dynamiquement au changement de thème. |
| **Ligne de Section Documentaire** | `SectionRowWidget` | Checkbox tristate synchro `QTreeWidgetItem`<br>Badges niveau H1 (indigo `ACCENT_*`) / H2 (bleu `COLOR_BLUE_*`) / H3+ (gris)<br>Titre + plage pages `TEXT_MUTED`<br>Badges « N mots » (jaune si <25), « N cartes » (accent), diagnostic (Utile/Exclu/Quasi vide) | `ACCENT_BG`, `COLOR_PURPLE_TEXT`, `ACCENT_BORDER`, `COLOR_BLUE_BG/TEXT/BORDER`, `TEXT_MUTED`, `TEXT_PRIMARY`, `COLOR_YELLOW`, `BG_ACTIVE`, `ACCENT_PRIMARY`<br>`COLOR_GREEN_BG/TEXT/BORDER`, `COLOR_RED_BG/TEXT/BORDER`, `COLOR_YELLOW_BG/TEXT/BORDER` | Ligne personnalisée d'un nœud d'arbre de document dans la délimitation de portée : diagnostic de section (niveau, volume, cartes existantes, recommandation) et inclusion/exclusion tristate. |
| **Barre d'Étendue de Portée** | `ScopeRangeBarWidget` | Rendu pur QPainter<br>Piste arrondie `BG_INPUT` + bordure<br>Segment actif proportionnel accent<br>Texte « Portée : Pages X à Y (N / Total) » | `BG_INPUT`, `BORDER_COLOR`, `ACCENT_PRIMARY`, `FONT_MAIN`<br>Texte blanc (contraste sur segment accent) | Barre visuelle représentant l'étendue totale d'un document et la plage utile demandée, centro-fort en contexte de délimitation de génération. |
| **Visionneuse de Document Intégrée** | `DocumentPreviewWidget` | Carte `QFrame#previewCard` + en-tête `#previewHeader`<br>Badge type PDF/Album/Markdown + toggle PDF/Markdown<br>Navigation pages ‹ ›, zoom −/Ajuster/+<br>Badge inclusion/exclusion page courante (`lbl_scope_status`) | `BG_PANEL`, `BORDER_COLOR`, `RADIUS_MD`, `BG_INPUT`, `TEXT_PRIMARY`, `BG_ACTIVE`, `ACCENT_PRIMARY`, `TEXT_MUTED`, `FONT_MAIN`<br>`COLOR_GREEN_BG/TEXT/BORDER` (inclus), `COLOR_RED_BG/TEXT/BORDER` (exclu)<br>Fonds visionneuse `#1e1e2e`/`#0f172a` + CSS Markdown injecté (hors tokens, rendu document) | Rendu de document synchronisé avec la portée : PDF natif multipages (`QPdfView`), Markdown stylisé avec ancres, planches haute résolution album, et indicateur dynamique d'inclusion de la page courante. |

---

# 🌓 PARTIE 2 : Architecture Bivalente Sombre 🌙 / Clair ☀️

L'interface repose sur une séparation claire entre :
1. **Le Mode d'Apparence :** 🌙 Sombre ou ☀️ Clair.
2. **La Famille de Thème :** L'une des 12 identités graphiques d'AnkiForge.

Lorsqu'un utilisateur change de mode ou de thème :
* Le `StyleEngine` bascule vers la variante (sombre ou claire) de la famille active via `set_color_mode()` ou `toggle_color_mode()`.
* `DesignTokens` est réassigné à chaud.
* La palette Qt (`QPalette`) et la feuille de style QSS globale sont réinjectées sur `QApplication`.
* Les ombres portées s'adaptent dynamiquement (45% d'opacité en mode sombre, 12% en mode clair).

---

# 🎨 PARTIE 3 : Les 12 Familles de Thèmes Bivalentes (24 Thèmes)

| Famille | Identifiant | Icône | Description | 🌙 Variante Sombre | ☀️ Variante Claire |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **JetBrains IDE** | `jetbrains` | `ph.code` | Style IDE haute densité avec accent Indigo vif. | `JETBRAINS_DARK` (`ide`) | `JETBRAINS_LIGHT` (`jetbrains_light`) |
| **Modern SaaS Emerald** | `emerald` | `ph.chart-line-up` | Tableau de bord contemporain avec accent vert Émeraude. | `EMERALD_DASHBOARD` (`dashboard`) | `EMERALD_LIGHT` (`emerald_light`) |
| **Cyber Glass** | `glassmorphism` | `ph.sparkle` | Verre dépoli translucide et reflets Améthyste. | `CYBER_GLASS` (`glassmorphism`) | `CYBER_GLASS_LIGHT` (`cyber_glass_light`) |
| **Apple macOS Native** | `macos` | `ph.apple-logo` | Épure et élégance Apple avec accent Bleu Cupertino. | `MACOS_DARK` (`macos`) | `MACOS_LIGHT` (`macos_light`) |
| **Synthwave '84** | `synthwave` | `ph.sun-horizon` | Rétro-futurisme néon et ambiance coucher de soleil. | `SYNTHWAVE_84` (`synthwave_84`) | `SYNTHWAVE_LIGHT` (`synthwave_light`) |
| **Monokai Pro** | `monokai` | `ph.lightning` | Contraste précis de référence avec accent Or / Ambre. | `MONOKAI_PRO` (`monokai_pro`) | `MONOKAI_LIGHT` (`monokai_light`) |
| **Catppuccin Pastel** | `catppuccin` | `ph.coffee` | Harmonie pastel apaisante (Mocha & Latte). | `CATPPUCCIN_MOCHA` (`catppuccin_mocha`) | `CATPPUCCIN_LATTE` (`catppuccin_latte`) |
| **Nord Arctic** | `nord` | `ph.snowflake` | Clarté scandinave ardoise et cyan (Polar & Snow Storm). | `NORD_POLAR` (`nord_polar`) | `NORD_LIGHT` (`nord_light`) |
| **Dracula / Alucard** | `dracula` | `ph.moon-stars` | Style gothique vampire sombre et brume mauve claire. | `DRACULA_OFFICIAL` (`dracula_official`) | `DRACULA_LIGHT` (`dracula_light`) |
| **Tokyo Night / Day** | `tokyo` | `ph.buildings` | Lumières tokyoïtes nocturnes et éclat matinal. | `TOKYO_NIGHT` (`tokyo_night`) | `TOKYO_DAY` (`tokyo_day`) |
| **Solarized** | `solarized` | `ph.sun` | Précision colorimétrique éprouvée bleu canard et parchemin. | `SOLARIZED_DARK` (`solarized_dark`) | `SOLARIZED_LIGHT` (`solarized_light`) |
| **One Pro Atom** | `one_pro` | `ph.atom` | Le standard Atom/VSCode neutre et équilibré. | `ONE_DARK_PRO` (`one_dark_pro`) | `ONE_LIGHT_PRO` (`one_light_pro`) |

---

# 📐 PARTIE 4 : Les Dispositions d'Interface (Layouts)

AnkiForge supporte **4 architectures de disposition** interchangeables à chaud (`LayoutManager`) :

1. **Concept IDE (`ide`) :** Barre latérale rétractable (68px à 260px), multi-fenêtrage détachable (`IdePanel`), omnibox `Ctrl+K`.
2. **Concept Dashboard (`dashboard`) :** Présentation en grille réactive sans barre latérale permanente, cartes d'actions rapides.
3. **Concept Glassmorphism (`glassmorphism`) :** Navigation par pilules flottantes, conteneurs effet verre givré.
4. **Concept macOS (`macos`) :** Barre supérieure compacte (54px) avec sélecteur segmenté horizontal.
